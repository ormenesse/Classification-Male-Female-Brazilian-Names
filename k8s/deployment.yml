apiVersion: apps/v1
kind: Deployment
metadata:
  name: lidfem-app
  namespace: data-science
spec:
  selector:
    matchLabels:
      app: lidfem-app
  replicas: 1
  template:
    metadata:
      labels:
        app: lidfem-app
    spec:
      terminationGracePeriodSeconds: 70
      containers:
        - name: lidfem-app
          image: 881379168558.dkr.ecr.us-west-2.amazonaws.com/lidfem-app:latest
          imagePullPolicy: Always
          lifecycle:
            preStop:
              exec:
                command: ["sleep","40"]
          resources:
            limits:
              cpu: "0.5"
              memory: "256Mi"
            requests:
              cpu: "0.1"
              memory: "128Mi"
          ports:
            - containerPort: 5000
